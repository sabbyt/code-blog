$(document).ready(function() {
  makeNewArticle.renderFromEditButt();
  makeNewArticle.updateEntry();
  makeNewArticle.addEntry();
  // blog.fetchFromDB();

  // blog.initArticleEditorPage();

  // blog.handleAddButton();
  // blog.handleDeleteButton();
});
